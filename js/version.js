// Bump these two with every shipped update so the app can display
// what's actually installed — the one thing a re-upload can't verify
// on its own. Keep VERSION as a simple integer-dot count of updates
// shipped (matches the README's "Update N" changelog numbering).
export const APP_VERSION = "12";
export const LAST_UPDATED = "17 Aug 2026";
